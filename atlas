#!/usr/bin/env ruby

require_relative "repl.rb"

# only intented for docs test to print things like it was in repl
if ARGV.include?("-repl_mode")
  $repl_mode = true
  ARGV.delete("-repl_mode")
end

repl
